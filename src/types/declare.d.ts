export declare module "@mui/material" {
  interface TypeBackground {
    main: string;
    lightGray: string;
  }

  interface TypeText {
    lightGray: string;
  }

  interface Palette {
    system: string;
  }

  interface PaletteOptions {
    system?: string;
  }
}
